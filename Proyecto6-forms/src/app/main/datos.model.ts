export interface ImpresoraModel {
    id: string | number;
    nombre: string;
}

export interface DepartamentoModel {
    id: string | number;
    nombre: string;
}

export interface UsuarioModel {
    nombre: string;
    apellidos: string;
    telefono: string;
}